require "spec_helper"

describe Pray::Remote do
  it "has a version number" do
    expect(Pray::Remote::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
